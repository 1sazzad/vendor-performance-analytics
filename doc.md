# ETL process
it stands for Extract-> Transform-> Load
it used to collect data from different sources, clean and process it, and store it in a system where it can be analyzed
in this project I used ETL process. First collected the data from CSV files then processed data for furture analysis after load the reusable data into SQLite